# AGENT 1 TASK — Project Scaffolding (myTrack)

You are Agent 1 for the myTrack project. Read this ENTIRE task before writing any code. PLAN your approach first, then build.

## YOUR JOB
Complete the Next.js 14 project scaffolding. The project was partially created via create-next-app in the current directory. Dependencies are installed. You need to finish the setup.

## WHAT ALREADY EXISTS
- Next.js 14 project with App Router in current directory
- node_modules with: next, react, react-dom, @google/genai, @clerk/nextjs, @supabase/supabase-js, nanoid, typescript
- src/lib/types.ts (TypeScript interfaces — DO NOT MODIFY)
- src/lib/supabase.ts (Supabase client — DO NOT MODIFY)
- src/lib/gemini.ts (Gemini client — DO NOT MODIFY)
- src/app/globals.css (CSS custom properties — DO NOT MODIFY)
- vercel.json (configured — DO NOT MODIFY)
- .env.local (template — DO NOT MODIFY)

## WHAT YOU MUST CREATE

### 1. src/app/layout.tsx
- Import ClerkProvider from @clerk/nextjs, wrap the entire app
- Load IBM Plex Sans and IBM Plex Mono via next/font/google
- Set CSS variables: --font-ibm-plex-sans and --font-ibm-plex-mono on the body className
- Import globals.css
- HTML lang="en", metadata: title "myTrack", description "AI music, made for you."

### 2. middleware.ts (in project root, NOT in src)
- Import clerkMiddleware and createRouteMatcher from @clerk/nextjs/server
- Protect these routes: /dashboard, /generate, /library, /playlists, /api/songs/(.*), /api/spotify/(.*), /api/generate/(.*), /api/playlists/(.*)
- Public routes: /, /sign-in(.*), /sign-up(.*), /share/(.*)
- Export config with matcher that excludes static files and _next

### 3. Sign-in and Sign-up pages
- src/app/sign-in/[[...sign-in]]/page.tsx — render SignIn routing="path" from @clerk/nextjs, centered on page with inline styles
- src/app/sign-up/[[...sign-up]]/page.tsx — render SignUp routing="path" from @clerk/nextjs, centered on page with inline styles

### 4. ALL page stubs (placeholder divs — other agents will fill them)
Create these as 'use client' components with a simple placeholder div:
- src/app/page.tsx — Landing page placeholder (REPLACE the default Next.js page)
- src/app/dashboard/page.tsx
- src/app/generate/page.tsx
- src/app/library/page.tsx
- src/app/playlists/page.tsx
- src/app/playlists/[id]/page.tsx
- src/app/share/[id]/page.tsx (this one is NOT 'use client', it's a server component)

### 5. ALL API route stubs
Create Next.js App Router route handlers. Each returns NextResponse.json with a placeholder. Use import { NextRequest, NextResponse } from 'next/server'.
- src/app/api/spotify/sync/route.ts (POST)
- src/app/api/generate/ideas/route.ts (POST)
- src/app/api/generate/prompt/route.ts (POST)
- src/app/api/generate/music/route.ts (POST)
- src/app/api/generate/cover/route.ts (POST)
- src/app/api/generate/queue/route.ts (POST)
- src/app/api/generate/embed/route.ts (POST)
- src/app/api/songs/route.ts (GET)
- src/app/api/songs/[id]/route.ts (GET, PATCH, DELETE)
- src/app/api/songs/[id]/play/route.ts (POST)
- src/app/api/songs/[id]/rate/route.ts (POST)
- src/app/api/songs/search/route.ts (POST)
- src/app/api/playlists/route.ts (GET, POST)
- src/app/api/playlists/[id]/route.ts (PATCH, DELETE)
- src/app/api/playlists/[id]/songs/route.ts (GET, POST, PATCH)
- src/app/api/playlists/[id]/songs/[songId]/route.ts (DELETE)

### 6. Cleanup
- Delete src/app/page.module.css if it exists

## STYLING RULES (PRD section 16)
- Font: IBM Plex Sans for all UI, IBM Plex Mono for code/lyrics only
- No Tailwind, no CSS modules, no MUI — inline style objects only
- No rounded corners on structural elements
- CSS vars: var(--ink), var(--bg), var(--border), var(--card-bg), etc.

## IMPORTANT
- Use TypeScript for ALL files
- Use Next.js App Router conventions (route.ts for API, page.tsx for pages)
- Do NOT install any new packages
- Make sure it builds cleanly
